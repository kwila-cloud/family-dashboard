# Example Setup

This directory is an example of the setup directory expected by the installation script.

There must be:

- `config/config.js`
  - [Magic Mirror configuration](https://docs.magicmirror.builders/configuration/introduction.html)
- `modules.json`
  - A mapping of module names to module git repos
